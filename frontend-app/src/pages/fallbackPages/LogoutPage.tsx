export default function LogoutPage() {
  return (
    <div id="logout-page">
      <h1>Stop!</h1>
      <p>You need to be logged out to register a new account or to login!</p>
      <p>
        <i>Logout required</i>
      </p>
    </div>
  );
}
